; vim:ft=lisp

;
; These are just examples of n7 forms
; strictly for (experiment|inspir)ation
;

(def sym? (x)
  (type? 'SYMBOL x))

(def nil? (x)
  (eq x nil))

;(def (call &rest args)
;  (apply args))

(def (++ n)
  (+ 1 n))

(def (-- n)
  (- n 1))

(sym? x)
(nil? x)
(eql x y)

(print "Hello, World")

(dump '(1 2 3 4))
